#include "pipeline/backend/backend.h"

// cv::Mat& MattingBackend::postprocess(const cv::Mat& original, const TensorData& output) {
// 	arcforge::embedded::utils::Logger::GetInstance().Info("Postprocessing started.");
// 	TensorData tmp_data = output;

// 	return original;
// }