pyyaml
requests
yapf
# GPU variant (CUDA 11.8) — for training on RTX 3090 (sm_86).
# Requires PyTorch official WHL index, NOT pypi/tsinghua:
#   pip install torch==2.0.1+cu118 torchvision==0.15.2+cu118 --index-url https://download.pytorch.org/whl/cu118
# helmsman prepare will detect GPU and use the correct index automatically.
# For CPU-only (ONNX export, golden files, no GPU): use requirements-cpu.txt instead.
torch==2.0.1+cu118
torchvision==0.15.2+cu118
onnx==1.8.1
onnxruntime==1.6.0
# opencv-python: If error occurs, cv-headless will be installed replaced
opencv-python==4.5.1.48
# Pin protobuf to a compatible version to avoid API breaking changes in 4.x
protobuf==3.20.1
# Pin numpy to a version BEFORE np.object was removed
numpy==1.21.6




# ---------------------
# Retrain Used
scipy==1.10.1

