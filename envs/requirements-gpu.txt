pyyaml
requests
yapf
# GPU variant (CUDA 11.8) — for training on RTX 3090 (sm_86).
# Upgraded from 1.7.1+cu102 to 2.0.1+cu118 for sm_86 compatibility.
# torch+cu118 must be installed from PyTorch WHL index, NOT pypi/tsinghua.
# helmsman prepare detects GPU via nvidia-smi and uses this file automatically.
torch==2.0.1+cu118
torchvision==0.15.2+cu118
onnx==1.8.1
onnxruntime==1.6.0
# opencv-python: If error occurs, cv-headless will be installed replaced
opencv-python==4.5.1.48
# Pin protobuf to a compatible version to avoid API breaking changes in 4.x
protobuf==3.20.1
# Pin numpy to a version BEFORE np.object was removed
numpy==1.21.6




# ---------------------
# Retrain Used
scipy==1.10.1

