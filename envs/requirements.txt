pyyaml
requests
yapf
# CPU-only torch — for ONNX export, golden file generation, inference validation.
# No GPU required. Installed from PyTorch WHL CPU index automatically by helmsman prepare.
# For GPU training (RTX 3090): use requirements-gpu.txt instead.
torch==2.0.1
torchvision==0.15.2
onnx==1.14.1
onnxruntime==1.15.1
# onnx-simplifier: graph optimization for ONNX models (folds Shape/Slice/Concat chains,
# eliminates redundant nodes). Used by RVM Phase-1 Block 1.1 onnxsim step.
# Compatible with onnx>=1.11; environment-agnostic (no GPU/CPU dependency).
onnx-simplifier==0.4.35
# opencv-python: If error occurs, cv-headless will be installed replaced
opencv-python==4.5.1.48
# Pin protobuf to a compatible version to avoid API breaking changes in 4.x
# 3.20.2: minimum version required by onnx==1.14.1 (>=3.20.2); stays below 4.x breaking API
protobuf==3.20.2
# Pin numpy to a version BEFORE np.object was removed
numpy==1.21.6




# ---------------------
# Retrain Used
scipy==1.10.1

